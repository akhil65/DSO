services:
  waf:
    image: owasp/modsecurity-crs:nginx
    ports:
      - "3001:8080"
    environment:
      - BACKEND=http://host.docker.internal:3000
      - PORT=8080
      - MODSEC_RULE_ENGINE=On
      - BLOCKING_PARANOIA=1
